\section{Discussion and Future Work}

Projection--Carry Geometry suggests several extensions. One can study carry
complexity as a function of sparse support geometry, compare binary and large
base multiplication, or replace the two-factor matrix by a higher-order tensor
for multiplication of several numbers.

A central direction is to model carry as nonlinear mass flow on the degree
axis, with anti-diagonal projection providing the linear input state.

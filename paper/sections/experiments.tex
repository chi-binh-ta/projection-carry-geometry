\section{Experiments}

The accompanying Python code includes demos for decimal and binary
multiplication, sparse digit support experiments, carry mass distributions, and
base-dependence comparisons. The generated figures show interaction matrices,
raw diagonal sums, carry profiles, and support sumsets.

The experiments are designed as small reproducible probes rather than final
benchmarks.
